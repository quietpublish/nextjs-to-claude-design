// A Tailwind-styled component (no CSS module) — exercises the `--tailwind` CSS path.
export default function Brand() {
  return <div className="bg-brand text-white inline-flex items-center rounded-lg px-4 py-2">Brand</div>;
}
